# Core rule: KISS + DRY

KISS: each skill solves exactly one thing, in the most direct way possible, using what already exists before proposing something new.

DRY: a rule or instruction exists in one place only. Everything else references it, never copies it.
