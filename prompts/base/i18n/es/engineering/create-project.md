# Crear proyecto desde cero
Nombre: `{{PROJECT_NAME}}` · Stack: `{{STACK}}` · Descripción: `{{DESCRIPTION}}`
Ockham: ver `ockham-core.md`.

Entrega en orden: estructura de carpetas (con razón) · esquema DB (tablas/relaciones/índices) · contratos de API (request/response ejemplo) · implementación base · README de 3 pasos.

Solo si `{{DOCKER}}` es `"true"`: agregar Dockerfile y docker-compose.

Nunca generar sin pedido explícito: `{{FORBIDDEN}}`

Restricciones adicionales: `{{CONSTRAINTS}}`
