# Create Project from Scratch

Name: `{{PROJECT_NAME}}` · Stack: `{{STACK}}` · Description: `{{DESCRIPTION}}`
Ockham: see `ockham-core.md`.

Deliver in order: folder structure (with justification) · DB schema (tables/relations/indexes) · API contracts (request/response examples) · base implementation · 3-step README.

Only if `{{DOCKER}}` is `"true"`: add Dockerfile and docker-compose.

Never generate without explicit request: `{{FORBIDDEN}}`

Additional constraints: `{{CONSTRAINTS}}`
